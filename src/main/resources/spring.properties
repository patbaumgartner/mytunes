# Read by SpringProperties at runtime, inside the Wasm module as much as on the JVM.
# No bean definition uses a #{...} expression, so skipping SpEL support keeps its parser
# machinery out of the reachable set. The jar itself must stay on the classpath: Spring's
# AOT processing links its types unconditionally.
spring.spel.ignore=true
